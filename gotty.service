[Unit]
Description=GoTTY
After=network.target

[Service]
Type=simple
User=root
Environment="TERM=xterm-256color"
ExecStart=/usr/local/bin/gotty --port 8083 --permit-write --reconnect -- mc -a /mnt/staging_downloads /mnt/media_pool
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
